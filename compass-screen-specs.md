# Compass - 画面仕様書

**Version:** 1.0
**関連ドキュメント:** `compass-requirements.md`

---

## 目次

- [0. テンプレート（コピペ用）](#0-テンプレートコピペ用)
- [1. 認証系](#1-認証系)
  - [1.1 ログイン](#11-ログイン-scr-auth-001)
  - [1.2 サインアップ（招待トークン経由）](#12-サインアップscr-auth-002)
- [2. ダッシュボード](#2-ダッシュボード)
  - [2.1 HOME](#21-home-scr-home-001)
- [3. 商品・出品管理](#3-商品出品管理)
  - [3.1 出品商品一覧](#31-出品商品一覧-scr-list-001)
  - [3.2 商品詳細・編集](#32-商品詳細編集-scr-list-002)
  - [3.3 翻訳エディタ](#33-翻訳エディタ-scr-trans-001)
  - [3.4 一括出品実行](#34-一括出品実行-scr-publish-001)
- [4. 受注](#4-受注)
  - [4.1 受注一覧](#41-受注一覧-scr-order-001)
- [5. 設定](#5-設定)
  - [5.1 販売先設定](#51-販売先設定-scr-set-001)
  - [5.2 利益設定](#52-利益設定-scr-set-002)
- [6. 本部専用](#6-本部専用)
  - [6.1 加盟店管理](#61-加盟店管理-scr-hq-001)
- [7. 共通コンポーネント仕様](#7-共通コンポーネント仕様)

---

## 0. テンプレート（コピペ用）

新規画面を追加するときはこのテンプレートをコピーして埋める。

```markdown
### X.Y 画面名 (SCR-XXX-NNN)

#### 目的
（この画面が解決する課題を1〜2文で）

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-XXX-NNN |
| URLパス | `/path/to/page` |
| 認可ロール | owner / tenant_admin / tenant_member / tenant_operator |
| 主要ユースケース | （箇条書きで2〜4個） |
| 関連要件番号 | requirements.md の該当節 |

#### 画面レイアウト
（ASCIIまたは構造説明）

#### UI要素
| ID | 要素 | 種別 | データ | バリデーション | アクション |
|----|------|------|--------|----------------|-----------|

#### データソース
- 取得：（テーブル / API）
- 更新：（テーブル / API）

#### 状態
| 状態 | 表示 |
|------|------|
| 初期 | |
| ローディング | |
| 空 | |
| エラー | |

#### バリデーション・エラーケース
- （箇条書き）

#### イベント・遷移
- （ボタン押下時の遷移先・副作用）

#### アクセシビリティ
- （フォーカス順、キーボード操作等の特記）

#### 計測・ログ
- （送る監査ログイベント名、計測イベント）
```

---

## 1. 認証系

### 1.1 ログイン (SCR-AUTH-001)

#### 目的
登録済みユーザーが自テナントへサインインする。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-AUTH-001 |
| URLパス | `/login` |
| 認可ロール | 未認証 |
| 主要ユースケース | メール+パスワードログイン / Googleログイン / 2FA入力 / パスワードリセット導線 |
| 関連要件番号 | 3.2 |

#### 画面レイアウト
```
+----------------------------------+
|         [Compass ロゴ]           |
|                                  |
|         ログイン                  |
|                                  |
|   メールアドレス                  |
|   [____________________]         |
|                                  |
|   パスワード                      |
|   [____________________] 👁       |
|                                  |
|   [ ログイン ]  幅100%            |
|   ────── または ──────            |
|   [  G  Googleでログイン  ]       |
|                                  |
|   パスワードをお忘れの方          |
+----------------------------------+
```

#### UI要素
| ID | 要素 | 種別 | データ | バリデーション | アクション |
|----|------|------|--------|----------------|-----------|
| email | メールアドレス | text input | string | RFC5322 / 必須 | submitに紐づく |
| password | パスワード | password input | string | 8文字以上 / 必須 | submitに紐づく |
| toggle-pw | パスワード表示切替 | icon button | - | - | input typeを切替 |
| submit | ログイン | primary button | - | - | Supabase Auth呼出 |
| google | Googleログイン | secondary button | - | - | OAuth flow開始 |
| forgot | パスワード再設定 | link | - | - | `/forgot-password`へ |

#### データソース
- 取得：なし
- 更新：`auth.users`（Supabase Auth）

#### 状態
| 状態 | 表示 |
|------|------|
| 初期 | フォーム表示 |
| 送信中 | submitボタンをスピナー付きdisabled |
| 認証失敗 | フォーム下に赤字エラー |
| 2FA要求 | TOTPコード入力モーダル |

#### バリデーション・エラーケース
- 認証失敗：「メールアドレスまたはパスワードが正しくありません」（理由を分けない＝アカウント存在を漏らさない）
- アカウント停止中：「このアカウントは停止されています。本部にお問い合わせください。」
- レート制限：「短時間に試行が多すぎます。1分後に再度お試しください。」
- 2FAが有効：TOTPコード6桁入力 → 検証

#### イベント・遷移
- 成功 → `/`（HOME）
- 2FA成功 → `/`
- 「パスワードをお忘れ」→ `/forgot-password`

#### アクセシビリティ
- form要素にlabelを必ず関連付け
- エラーメッセージは `role="alert"`
- Enterで送信

#### 計測・ログ
- `auth.login.success` / `auth.login.failure` / `auth.2fa.required`
- 監査ログには記録しない（IPは別途認証ログテーブルへ）

---

### 1.2 サインアップ（SCR-AUTH-002）

#### 目的
本部または加盟店管理者から発行された招待トークンをもとにアカウントを作成する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-AUTH-002 |
| URLパス | `/signup?token=xxx` |
| 認可ロール | 未認証＋有効な招待トークン保持者 |
| 主要ユースケース | 招待トークン検証 / プロフィール入力 / 利用規約同意 |
| 関連要件番号 | 3.2、4.9.1 |

#### UI要素
| ID | 要素 | 種別 | バリデーション |
|----|------|------|----------------|
| token | 招待トークン | hidden | URLクエリから自動取得 |
| email | メール（表示のみ） | readonly text | 招待トークンから復元 |
| full-name | 氏名 | text | 必須・1〜100文字 |
| password | パスワード | password | 8文字以上＋数字＋英字 |
| password-confirm | パスワード（確認） | password | passwordと一致 |
| tos | 利用規約同意 | checkbox | チェック必須 |
| submit | アカウント作成 | primary button | - |

#### バリデーション・エラーケース
- トークン無効／期限切れ：エラーページにリダイレクト「招待が期限切れです。本部に再発行を依頼してください。」
- メール重複：「このメールアドレスは既に登録されています。ログインしてください。」

#### イベント・遷移
- 成功 → 2FA設定画面（管理者ロールの場合は強制）→ HOME

---

## 2. ダッシュボード

### 2.1 HOME (SCR-HOME-001)

#### 目的
運用状況をひと目で把握し、優先対応すべきタスクへ最短で到達する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-HOME-001 |
| URLパス | `/` |
| 認可ロール | tenant_admin / tenant_member（owner時は本部ダッシュボードに振分け） |
| 主要ユースケース | KPI確認 / エラー対応 / 為替確認 / お知らせ確認 |
| 関連要件番号 | 4.1 |

#### 画面レイアウト
```
+--------------------------------------------------------------+
| [サイドバー]  | HOME                              [更新ボタン] |
|              |                                                |
|              | ┌─KPIカード x6 (3列x2段) ──────────────────┐  |
|              | │ 本日出品  / 総出品 / 販売中 / 出品待ち     │  |
|              | │ エラー    / 本日受注 / 本日売上(KRW/JPY) │  |
|              | └────────────────────────────────────────────┘  |
|              |                                                |
|              | ┌─売上推移チャート (30日)──────────────────┐  |
|              | │  販路フィルタタブ: 全 / Coupang / Naver / 11st│
|              | │  [折れ線グラフ]                              │  |
|              | └────────────────────────────────────────────┘  |
|              |                                                |
|              | ┌─エラー通知────────┐ ┌─為替────┐ ┌─お知らせ┐|
|              | │ 直近10件          │ │JPY→KRW │ │本部通達│|
|              | │ クリックで詳細     │ │ 9.21   │ │ 3件   │|
|              | └────────────────────┘ └─────────┘ └────────┘|
+--------------------------------------------------------------+
```

#### UI要素
| ID | 要素 | データソース |
|----|------|--------------|
| kpi-listed-today | 本日出品数 | `channel_listings` where created_at::date = today |
| kpi-total-listed | 総出品数 | `channel_listings` count |
| kpi-live | 販売中 | status='live' |
| kpi-pending | 出品待ち | status in ('draft','pending') |
| kpi-errors | エラー件数 | `jobs` status='failed' last 24h |
| kpi-orders-today | 本日受注 | `orders` ordered_at::date = today |
| kpi-revenue-today | 本日売上 | `orders` 集計 |
| chart-revenue | 売上推移 | `orders` 30日集計 |
| list-errors | エラー一覧 | `jobs` failed |
| widget-fx | 為替 | `fx_rates` 最新 |
| widget-news | お知らせ | `announcements` |

#### データソース
- 取得：上記テーブル群（Supabase RPC `dashboard_summary(tenant_id)` で一括取得を推奨）
- 更新：なし
- リアルタイム：エラー通知のみ Supabase Realtime購読

#### 状態
| 状態 | 表示 |
|------|------|
| 初期ロード中 | KPIカードはskeleton |
| データ無し（出品0件） | オンボーディングCTA「最初の商品を登録する」 |
| 部分エラー | 該当ウィジェットのみエラー表示、他は通常 |

#### イベント・遷移
- KPIカードクリック → 該当のフィルタ済み一覧画面へ
- エラー行クリック → ジョブ詳細モーダル
- 為替ウィジェット → 利益設定画面

#### 計測・ログ
- `dashboard.viewed`、KPIカードクリック時 `dashboard.kpi_clicked` (kpi_id付き)

---

## 3. 商品・出品管理

### 3.1 出品商品一覧 (SCR-LIST-001)

#### 目的
登録された商品を多軸で絞り込み、一括操作（出品・編集・削除・エクスポート）を行う。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-LIST-001 |
| URLパス | `/listings?status=&channel=&page=` |
| 認可ロール | tenant_admin / tenant_member |
| 関連要件番号 | 4.3.1 |

#### 画面レイアウト
```
+-----------------------------------------------------------+
| 出品商品管理                                              |
| [+ 商品追加] [一括出品Excel DL] [スマートストア用Excel DL]|
|                                                           |
| ┌─フィルタバー──────────────────────────────────────────┐  |
| │ 検索: [____] 状態:[全▾] 販路:[全▾] 仕入元:[全▾]      │  |
| │ カテゴリ:[全▾] 価格:[__]〜[__] [テンプレート保存]      │  |
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| ページ件数 [20▾]    任意の件数 [設定]                     |
| < 前  1  2  3 ... 9  >                                    |
|                                                           |
| ┌─テーブル─────────────────────────────────────────────┐  |
| │ ☐ |画像|商品名 |マーケット|出品ステータス|価格|利益率|  |
| │ ☐ |🖼  |...   |Coupang  |出品待ち     |...|...   |   |
| │ ...                                                   │  |
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| 選択中: 3件  [一括出品][一括編集][CSVエクスポート][削除]  |
+-----------------------------------------------------------+
```

#### UI要素
| ID | 要素 | 仕様 |
|----|------|------|
| search | 検索ボックス | 商品名・SKU・ASIN・クーポンID部分一致、debounce 300ms |
| filter-status | 状態フィルタ | multi-select: draft / pending / approved / live / rejected / suspended |
| filter-channel | 販路フィルタ | coupang / naver / 11st |
| filter-source | 仕入元 | amazon / rakuten / yahoo |
| filter-template | テンプレート保存 | `filter_templates` へ保存 |
| page-size | ページ件数 | 20 / 50 / 100 |
| pagination | ページャ | サーバーサイドページング |
| select-all | 全選択 | 現ページの全行 |
| bulk-action-bar | 一括アクションバー | 1件以上選択で表示 |
| col-protected | 削除防止アイコン | `is_protected=true` の行に🔒 |

#### データソース
- 取得：`listings` JOIN `channel_listings` JOIN `source_products`
- 更新：bulk action経由で `listings.status`, `jobs` insertなど

#### 一括操作の仕様
| アクション | 挙動 |
|-----------|------|
| 一括出品 | `jobs` に publish job を投入し進捗ページへ遷移 |
| 一括編集 | 編集モーダル（タイトル接頭辞・利益率・カテゴリ等のフィールド単位） |
| CSVエクスポート | 現在のフィルタ条件で全件エクスポート（ジョブ化、完了後DL通知） |
| 削除 | 確認モーダル必須、`is_protected=true` を含む場合は除外して実行＋警告表示 |

#### バリデーション・エラーケース
- 1000件超の選択は警告ダイアログ「処理に時間がかかります。続行しますか？」
- 削除時に保護対象が混在 → 「3件は削除防止対象のためスキップされました」と結果トースト

#### 計測・ログ
- `listings.viewed`、`listings.bulk_action` (action名+件数)
- 削除・編集は `audit_logs` に必須記録

---

### 3.2 商品詳細・編集 (SCR-LIST-002)

#### 目的
1商品の全情報を確認・編集し、販路ごとの出品状態を管理する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-LIST-002 |
| URLパス | `/listings/[id]` |
| 認可ロール | tenant_admin / tenant_member（operatorは限定編集のみ） |
| 関連要件番号 | 4.3.2 |

#### 画面レイアウト
```
+-----------------------------------------------------------+
| ← 一覧へ戻る                            [削除][複製][保存]|
|                                                           |
| 商品名(JA): ...                          [🔒 削除防止]    |
| 商品名(KO): ...                                            |
|                                                           |
| ┌─タブ──────────────────────────────────────────────┐    |
| │ [基本情報] [画像] [翻訳] [価格] [Coupang] [Naver] [11st]│
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| (タブの中身)                                              |
|                                                           |
| ┌─価格シミュレーター────────────────────────────────┐    |
| │ 仕入価格: 1,000円                                   │    |
| │ + 国際送料: 200円  + 関税: 80円  + VAT: 130円       │    |
| │ + 利益(30%): 423円                                  │    |
| │ = 販売価格: 1,833円 → 16,884 KRW (@9.21)            │    |
| └──────────────────────────────────────────────────────┘  |
+-----------------------------------------------------------+
```

#### タブ構成
| タブ | 内容 |
|------|------|
| 基本情報 | 商品名/ブランド/JAN/カテゴリ/属性 |
| 画像 | 並び替え（DnD）/ 差替 / 削除 / アップロード |
| 翻訳 | 左右ペイン（JA/KO）、翻訳ボタン、翻訳メモリ候補 |
| 価格 | 利益率、為替、最終価格の試算 |
| Coupang | カテゴリID、必須属性、出品ステータス、エラー、再送信ボタン |
| Naver | 카테고리, 원산지, 제조사, A/S |
| 11st | displayCategoryNo, taxTypCd 他 |

#### バリデーション
- 商品名(KO)：最大100文字（Coupang制約）
- 画像：1〜10枚、各5MB以下、JPG/PNG/WEBP
- 価格：下限ガード（原価+送料割れ禁止）
- 必須属性：販路ごとのスキーマで動的生成（zod）

#### イベント
- 「保存」→ 楽観的ロック（`updated_at` 比較）で衝突検知
- 「Coupangへ再送信」→ ジョブ投入、進捗トースト

---

### 3.3 翻訳エディタ (SCR-TRANS-001)

#### 目的
未翻訳商品を一括翻訳し、必要に応じて手動修正する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-TRANS-001 |
| URLパス | `/translations` |
| 認可ロール | tenant_admin / tenant_member / tenant_operator |
| 関連要件番号 | 4.4.1 |

#### 画面レイアウト
```
+-----------------------------------------------------------+
| 未翻訳一括更新                                            |
|                                                           |
| 対象: 未翻訳のみ ☑  選択商品のみ ☐                       |
| エンジン: [DeepL ▾]   翻訳メモリ使用: ☑                   |
| 禁止ワード辞書: [標準セット▾]                             |
|                                                           |
| [プレビュー実行]  [本番実行]                              |
|                                                           |
| ┌─プレビューテーブル────────────────────────────────┐   |
| │ # | 商品名(JA)        | 商品名(KO案)     | 修正    │   |
| │ 1 | ナイキ エアマックス| 나이키 에어맥스   | [編集]  │   |
| │ ...                                                  │   |
| └─────────────────────────────────────────────────────┘   |
|                                                           |
| 翻訳済: 0/342件   推定費用: ¥215   推定時間: 2分          |
+-----------------------------------------------------------+
```

#### バリデーション・エラーケース
- DeepL APIクォータ不足：「今月の翻訳枠が不足しています。設定で枠を確認してください」
- 禁止ワードヒット：警告アイコン＋置換候補表示
- 失敗行のみ再実行可能

---

### 3.4 一括出品実行 (SCR-PUBLISH-001)

#### 目的
選択した商品を任意販路へ一括出品し、進捗をリアルタイム表示する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-PUBLISH-001 |
| URLパス | `/publish/[jobId]` |
| 認可ロール | tenant_admin / tenant_member |
| 関連要件番号 | 4.5 |

#### 画面レイアウト
```
+-----------------------------------------------------------+
| 一括出品ジョブ #J-2026-04-25-0042                         |
|                                                           |
| 販路: Coupang  対象: 234件                                |
| 状態: 実行中  進捗: ██████░░░░ 142/234 (60.6%)            |
| 開始: 14:23:01  経過: 4分18秒  推定残: 2分30秒            |
|                                                           |
| ┌─結果サマリ──────────────────────────────────────┐    |
| │ ✓ 成功: 138件  ⚠ 警告: 2件  ✗ 失敗: 2件  ⏳ 待機: 92件│ |
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| ┌─失敗一覧─────────────────────────────────────────┐    |
| │ # | 商品 | エラー理由              | アクション    │   |
| │ 1 | ABC | カテゴリ未マッピング      | [マッピング]  │   |
| │ 2 | XYZ | 必須属性「素材」未入力    | [編集]        │   |
| └─────────────────────────────────────────────────────┘   |
|                                                           |
| [一時停止] [中止] [失敗のみ再実行] [CSVダウンロード]      |
+-----------------------------------------------------------+
```

#### データソース
- `jobs` (1件) + `job_items` (子レコード)
- Supabase Realtime購読で進捗更新

#### 状態遷移
`queued → running → (paused) → succeeded / failed / cancelled`

---

## 4. 受注

### 4.1 受注一覧 (SCR-ORDER-001)

#### 目的
販路横断で受注を一覧し、配送ステータスを管理する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-ORDER-001 |
| URLパス | `/orders` |
| 認可ロール | tenant_admin / tenant_member |
| 関連要件番号 | 4.7 |

#### UI要素
| 要素 | 仕様 |
|------|------|
| ステータスタブ | 全 / 新規 / 確認済 / 出荷済 / 配達済 / キャンセル / 返品 |
| フィルタ | 販路 / 期間 / 検索（注文番号・購入者名） |
| テーブル列 | 注文日 / 販路 / 注文番号 / 商品名 / 数量 / 金額 / PCCC / 配送状態 / アクション |
| アクション | 詳細 / 出荷登録（追跡番号入力） / キャンセル |

#### バリデーション
- PCCC：韓国の個人通関固有符号（P+12桁）形式チェック
- 追跡番号：選択業者ごとの形式（例：CJ대한통운は10〜12桁数値）

---

## 5. 設定

### 5.1 販売先設定 (SCR-SET-001)

#### 目的
Coupang / Naver / 11st の API認証情報を登録し接続テストする。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-SET-001 |
| URLパス | `/settings/channels` |
| 認可ロール | tenant_admin のみ |
| 関連要件番号 | 4.8.5 |

#### 画面レイアウト
```
+-----------------------------------------------------------+
| 販売先設定                                                |
|                                                           |
| ┌─Coupang────────────────────────────────────────────┐   |
| │ 状態: ● 接続済み (最終確認: 4/25 12:34)              │   |
| │ Vendor ID:    [A012345           ]                   │   |
| │ Access Key:   [••••••••••••••••  ] [変更]            │   |
| │ Secret Key:   [••••••••••••••••  ] [変更]            │   |
| │ [接続テスト]  [保存]  [連携解除]                     │   |
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| ┌─Naver Smart Store────────────────────────────────────┐  |
| │ 状態: ○ 未接続                                        │  |
| │ Client ID / Client Secret / Refresh Token            │  |
| │ [接続]                                                │  |
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| ┌─11st───────────────────────────────────────────────┐    |
| │ ...                                                   │  |
| └──────────────────────────────────────────────────────┘  |
+-----------------------------------------------------------+
```

#### セキュリティ要件（重要）
- 入力値は送信時にHTTPS必須
- 保存時はサーバー側で `pgcrypto` 暗号化、DBには暗号文のみ
- 表示時は復号せず常にマスク（`••••`）。「変更」押下で新規入力欄に切替
- 接続テスト失敗時、エラー詳細はログのみ。画面には汎用メッセージ
- 監査ログ：`channel.credential.updated`（値は記録しない、誰がいつだけ）

#### バリデーション
- Vendor ID：英数字
- 接続テスト：各プラットフォームの安全なReadエンドポイントを呼び出し、200を期待

---

### 5.2 利益設定 (SCR-SET-002)

#### 目的
利益率・為替バッファ・カテゴリ別上書きを設定する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-SET-002 |
| URLパス | `/settings/margin` |
| 認可ロール | tenant_admin のみ |
| 関連要件番号 | 4.8.3 |

#### UI要素
| 要素 | 仕様 |
|------|------|
| 既定利益率 | 0〜100%、小数1桁 |
| 為替レート取得元 | 手動 / Open Exchange Rates API |
| 為替バッファ | 0〜10%、小数1桁 |
| 端数処理 | 切り上げ / 切り下げ / 100KRW単位 / 1000KRW単位 |
| カテゴリ別上書き | カテゴリ＋利益率のテーブル（追加/削除/編集） |

#### プレビュー
- 「サンプル商品でシミュレーション」ボタン → モーダルで原価入力 → 計算結果表示

---

## 6. 本部専用

### 6.1 加盟店管理 (SCR-HQ-001)

#### 目的
本部が全加盟店の状態と利用状況を一元管理する。

#### 基本情報
| 項目 | 値 |
|------|-----|
| 画面ID | SCR-HQ-001 |
| URLパス | `/admin/tenants` |
| 認可ロール | owner のみ |
| 関連要件番号 | 4.9.1, 4.9.2 |

#### 画面レイアウト
```
+-----------------------------------------------------------+
| 加盟店管理                              [+ 加盟店招待]    |
|                                                           |
| フィルタ: 状態[全▾] プラン[全▾]  検索[__]                 |
|                                                           |
| ┌──テーブル──────────────────────────────────────────┐  |
| │店舗名|プラン|状態|出品数|今月売上|エラー率|契約期限|… │  |
| └──────────────────────────────────────────────────────┘  |
|                                                           |
| 行クリック → ドロワーで詳細・操作（停止・プラン変更等）    |
+-----------------------------------------------------------+
```

#### 操作
- 招待発行（メール送信＋トークン生成）
- プラン変更
- 一時停止 / 復帰 / 解約
- 代理ログイン（impersonation）：監査ログに必ず記録、セッションに警告バナー常時表示

---

## 7. 共通コンポーネント仕様

### 7.1 サイドバー
- 折りたたみ可（アイコンのみ表示モード）
- メニュー：HOME / 商品データ収集 / 管理 / 各種設定
- 「管理」「各種設定」は折りたたみアコーディオン
- ロールに応じて項目を非表示

### 7.2 トースト通知
- 種別：success / info / warning / error
- 自動消去：success/infoは4秒、warning/errorは手動
- 重要操作（一括削除等）はトーストではなくモーダルで結果表示

### 7.3 確認ダイアログ
- 破壊的操作（削除・停止・解約）は必須
- 影響範囲の数値を必ず表示（例：「234件の出品が停止されます」）
- 1000件超は二重確認（「DELETE」と入力させる）

### 7.4 データテーブル
- カラム表示切替・並び替え（ユーザーごとにLocalStorage保存）
- 列ピン留め
- 仮想スクロール（`@tanstack/react-virtual`）

### 7.5 空状態（Empty State）
- 必ずイラスト＋ガイド文＋次アクションボタンの3点セット
- 例：「まだ商品がありません」「商品データ収集から始めましょう」「[商品を追加]」

### 7.6 ローディング
- 一覧：skeleton row
- フォーム送信中：ボタンスピナー＋disabled
- ページ遷移：上部プログレスバー（NextTopLoader）

### 7.7 エラーバウンダリ
- ページ単位で配置
- 「再読み込み」「サポートに連絡」ボタン
- Sentryへ自動送信、エラーIDを画面表示

### 7.8 アクセシビリティ共通
- WCAG 2.1 AA準拠目標
- フォーカスリング常時表示
- カラーコントラスト 4.5:1 以上
- スクリーンリーダー対応のlandmark設置

---

## 付録A：URL設計規約

| パターン | 用途 |
|---------|------|
| `/[resource]` | 一覧 |
| `/[resource]/[id]` | 詳細 |
| `/[resource]/new` | 新規作成 |
| `/settings/[area]` | 設定 |
| `/admin/[resource]` | 本部専用 |

クエリパラメータでフィルタ・ページング状態を保持し、URL共有可能にする。

---

## 付録B：デザイントークン（暫定）

```
Primary:    #14B8A6 (Teal-500) - 既存ロゴ準拠
Secondary:  #1E293B (Slate-800)
Success:    #10B981
Warning:    #F59E0B
Error:      #EF4444
Info:       #3B82F6

Spacing:    4 / 8 / 12 / 16 / 24 / 32 / 48 / 64
Radius:     sm=4, md=8, lg=12, xl=16
Font:       Inter (latin), Noto Sans JP (ja), Noto Sans KR (ko)
```

---

**以上**
